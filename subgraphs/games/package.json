{
  "name": "games-subgraph",
  "version": "1.0.0",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "postbuild": "cp schema.graphql dist/",
    "start": "node dist/index.js",
    "dev": "ts-node --loader ts-node/esm index.ts"
  },
  "dependencies": {
    "@apollo/server": "^4",
    "@apollo/subgraph": "^2",
    "graphql": "^16"
  },
  "devDependencies": {
    "ts-node": "^10",
    "typescript": "^5"
  }
}
