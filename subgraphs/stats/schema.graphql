extend schema @link(url: "https://specs.apollo.dev/federation/v2.3", import: ["@key"])

type Stat @key(fields: "id") {
  id: ID!
  playerId: ID!
  gameId: ID!
  score: Int
}

type Query {
  stats: [Stat]
}